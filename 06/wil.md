CSS 기본형
선택자 {속성1: 속성값1; 속성2:속성값2;}

외부에서 가져올 때
<link rel="stylesheet" href="외부 스타일 시트 파일 경로">

전체 선택자
*{속성: 값; ...}

태그명 사용시
태그명{스타일 규칙}

클래스 사용시
태그 헤드에 클래스 언급
ex : .accent 클래스가 있을 경우 class="accent" 언급
아이디 사용시 class를 id로 교체

선택자1, 선택자2 {스타일 규칙}